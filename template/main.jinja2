<HTML>

    <table class="QuestionBox">
        <tr id="questionText">
        </tr>
        <tr>
 
            <td id="TestBox" onclick="test123()">
                eh? 

            </td>

        </tr>
        <tr>
            <td class="QuestionBox" onclick="questionSoundFile('resource/nh1.mp3')">
                ¿Cómo se dice 'sorry' en español? 
            </td>
        </tr>

        <tr>
            <td>
                <table class="AnswerButton" align="center">
                    <tr>
    {% for my_entry in range(DTO.questions|length) %}
                    
                        <td id="answer{{ my_entry }}" class="AnswerButton" onclick="soundfile('resource/vocab/{{ my_entry + 1001 }}-es.mp3')"' align="center">
                            {{ DTO.questions[my_entry] }}
                        </td>

        {% if (my_entry + 1) % 3 == 0 %}
                    </tr>
        {% endif %}

        {% if (my_entry + 1 != DTO.questions|length) %}
            {% if (my_entry + 1) % 3 == 0 %}
                    <tr>
            {% endif %}
        {% endif %}                    
    {% endfor %}

                </table>
            </td>
        </tr>
<!--  
        <tr>
            <td align="center">
                <button type="button">Audio</button>
                <button type="button">Quit</button>
            </td>
        </tr>
    </table>
--> 
</HTML>





